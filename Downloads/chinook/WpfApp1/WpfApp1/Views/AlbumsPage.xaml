<Page x:Class="WpfApp1.Views.AlbumsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:local="clr-namespace:WpfApp1.Views"
      mc:Ignorable="d"
      d:DesignHeight="450" d:DesignWidth="800"
      Title="AlbumsPage"
      Loaded="Page_Loaded">

	<Grid>
		<DockPanel>
			<TextBlock DockPanel.Dock="Top" Text="Chinook Albums"
                       Style="{StaticResource PageHeaderStyle}"/>

			<!-- Using GridView for albums -->
			<ListView x:Name="AlbumsListView">
				<ListView.View>
					<GridView>
						<GridViewColumn Header="ID" Width="50" DisplayMemberBinding="{Binding AlbumId}"/>
						<GridViewColumn Header="Title" Width="250" DisplayMemberBinding="{Binding Title}"/>
						<GridViewColumn Header="Artist" Width="200" DisplayMemberBinding="{Binding Artist.Name}"/>
					</GridView>
				</ListView.View>
			</ListView>
		</DockPanel>
	</Grid>
</Page>